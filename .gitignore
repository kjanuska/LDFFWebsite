node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Claude Code — personal/machine-specific files only
.claude/ide/
.claude/debug/

# Google Sheets API credentials (never commit these)
credentials.json
token.json

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Generated data (rebuilt from CSV — do not edit manually)
src/lib/data/*.json

# Sponsor logos live in static/images/sponsors/ (flat, no year subdirectory)
# Commit SVGs directly; ignore other formats if they are large/binary
static/images/sponsors/*.png
static/images/sponsors/*.jpg
